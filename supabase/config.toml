# Supabase Configuration for GEEF Project

# Project metadata is documented in docs/MCP_SUPABASE_GEEF.md.
# Supabase CLI does not support a custom [env.production] section here.

[api]
enabled = true
schemas = ["public", "storage"]

[db]
major_version = 15

[studio]
enabled = true

